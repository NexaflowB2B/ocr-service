name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  call-parent-ci:
    uses: NexaflowB2B/ci/.github/workflows/parent-ci.yaml@main
    secrets: inherit
